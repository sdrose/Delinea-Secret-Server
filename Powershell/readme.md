There is currently 2 issues - The current script does not export from safes with:
* Vault Names with special characters in the names
* Vault names with spaces before / after (which may need to be trimmed)
